/* ==========================================================================
   THE 26 CLUB — SETTINGS
   --------------------------------------------------------------------------
   Change the shared password below. Everyone who visits the site enters this
   one password to get in. Pick something easy to share with members.

   NOTE: This is light, front-end-only protection — good for keeping the page
   private, but the password sits in the site's code, so it is not strong
   security. Don't put anything truly sensitive behind it. Ask Claude Code to
   add real member logins (Google / Cloudflare Access) if you ever need that.
   ========================================================================== */
var CLUB_CONFIG = {
  password: "TryRugby-26Club-2026",   // <-- change this to your shared club password
  title:    "The 26 Club"
};
